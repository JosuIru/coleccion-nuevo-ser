
╔═══════════════════════════════════════════════════════════════════════════════╗
║                    BACKGROUND ROTATOR - EXTRACTION DIAGRAM                    ║
║                          REFACTORING v2.9.200 - FASE 1                        ║
╚═══════════════════════════════════════════════════════════════════════════════╝


ANTES (frankenstein-ui.js - 8,375 líneas)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│ class FrankensteinLabUI {                                               │
│                                                                          │
│   constructor() {                                                        │
│     this.organism = ...                                                  │
│     this.missionsSystem = ...                                            │
│     this.selectedMission = ...                                           │
│     ...                                                                  │
│     ┌──────────────────────────────────────────────────────────┐        │
│     │ 🔧 BACKGROUND ROTATION LOGIC (extraído)                  │        │
│     │ ─────────────────────────────────────────────────────    │        │
│     │ this.vintageBackgrounds = [...]  // 9 imágenes          │        │
│     │ this.backgroundRotationTimer = null                     │        │
│     │ this.previousBackgroundIndex = -1                       │        │
│     └──────────────────────────────────────────────────────────┘        │
│     this.microSocietyEvents = ...                                        │
│     ...                                                                  │
│   }                                                                      │
│                                                                          │
│   ┌────────────────────────────────────────────────────────────┐        │
│   │ 🔧 MÉTODOS EXTRAÍDOS (líneas 974-1036)                     │        │
│   │ ────────────────────────────────────────────────────────   │        │
│   │ setRandomDaVinciBackground(preferredImage) {              │        │
│   │   // 32 líneas de lógica                                  │        │
│   │   // - Selección aleatoria sin repetición                │        │
│   │   // - Preloading de imágenes                            │        │
│   │   // - Fallback en errores                               │        │
│   │ }                                                         │        │
│   │                                                           │        │
│   │ resolveAssetUrl(assetPath) {                             │        │
│   │   // 17 líneas de lógica                                 │        │
│   │   // - Resolución de URLs                                │        │
│   │   // - Múltiples fallbacks                               │        │
│   │ }                                                         │        │
│   │                                                           │        │
│   │ startBackgroundRotation(forceImage) {                    │        │
│   │   // 12 líneas de lógica                                 │        │
│   │   // - Control de intervalo                              │        │
│   │   // - Limpieza previa                                   │        │
│   │ }                                                         │        │
│   └────────────────────────────────────────────────────────────┘        │
│                                                                          │
│   // ... otros 200+ métodos                                             │
│ }                                                                        │
└─────────────────────────────────────────────────────────────────────────┘


DESPUÉS (frankenstein-ui.js - 8,315 líneas + BackgroundRotator - 151 líneas)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│ frankenstein-ui.js                                                       │
│ ────────────────────────────────────────────────────────────────────    │
│                                                                          │
│ import BackgroundRotator from './frankenstein/utils/...js';  ← NUEVO    │
│                                                                          │
│ class FrankensteinLabUI {                                               │
│   constructor() {                                                        │
│     this.organism = ...                                                  │
│     this.missionsSystem = ...                                            │
│     this.selectedMission = ...                                           │
│     ...                                                                  │
│     ┌──────────────────────────────────────────────────────────┐        │
│     │ ✅ DELEGACIÓN A MÓDULO EXTERNO                           │        │
│     │ ─────────────────────────────────────────────────────    │        │
│     │ this.backgroundRotator = new BackgroundRotator(         │        │
│     │   '--da-vinci-bg',                                       │        │
│     │   [...9 imágenes...]                                     │        │
│     │ );                                                       │        │
│     └──────────────────────────────────────────────────────────┘        │
│     this.microSocietyEvents = ...                                        │
│     ...                                                                  │
│   }                                                                      │
│                                                                          │
│   ┌────────────────────────────────────────────────────────────┐        │
│   │ ✅ MÉTODOS DEPRECADOS (wrappers para compatibilidad)      │        │
│   │ ────────────────────────────────────────────────────────   │        │
│   │ setRandomDaVinciBackground(img) {                         │        │
│   │   this.backgroundRotator.setRandomBackground(img);        │        │
│   │ }                                                          │        │
│   │                                                            │        │
│   │ resolveAssetUrl(path) {                                   │        │
│   │   return this.backgroundRotator.resolveAssetUrl(path);    │        │
│   │ }                                                          │        │
│   │                                                            │        │
│   │ startBackgroundRotation(img) {                            │        │
│   │   this.backgroundRotator.startRotation(img, 45000);       │        │
│   │ }                                                          │        │
│   └────────────────────────────────────────────────────────────┘        │
│                                                                          │
│   destroy() {                                                            │
│     ┌──────────────────────────────────────────────────────────┐        │
│     │ ✅ CLEANUP DELEGADO                                      │        │
│     │ ─────────────────────────────────────────────────────    │        │
│     │ if (this.backgroundRotator) {                            │        │
│     │   this.backgroundRotator.destroy();                      │        │
│     │ }                                                         │        │
│     └──────────────────────────────────────────────────────────┘        │
│     ...                                                                  │
│   }                                                                      │
│                                                                          │
│   // ... otros 200+ métodos                                             │
│ }                                                                        │
└─────────────────────────────────────────────────────────────────────────┘
                                        │
                                        │ imports
                                        ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ frankenstein-background-rotator.js (NUEVO)                              │
│ ────────────────────────────────────────────────────────────────────    │
│                                                                          │
│ const DEFAULT_BACKGROUNDS = [                                           │
│   'assets/backgrounds/vitruvio.jpg',                                    │
│   'assets/backgrounds/leonardo-skull.jpg',                              │
│   ...7 más                                                              │
│ ];                                                                       │
│                                                                          │
│ export class BackgroundRotator {                                        │
│   constructor(cssVariableName, backgroundImages) {                      │
│     this.cssVariableName = cssVariableName;                             │
│     this.backgrounds = backgroundImages || DEFAULT_BACKGROUNDS;         │
│     this.currentIndex = -1;                                             │
│     this.intervalId = null;                                             │
│   }                                                                      │
│                                                                          │
│   setRandomBackground(preferredImage) {                                 │
│     // Lógica de selección aleatoria                                    │
│     // Preloading con fallback                                          │
│     // Actualiza CSS variable                                           │
│   }                                                                      │
│                                                                          │
│   resolveAssetUrl(assetPath) {                                          │
│     // Conversión relativo → absoluto                                   │
│     // Manejo de múltiples casos                                        │
│   }                                                                      │
│                                                                          │
│   startRotation(forceImage, intervalMs = 45000) {                       │
│     this.stopRotation(); // Limpiar previo                              │
│     this.setRandomBackground(forceImage);                               │
│     this.intervalId = setInterval(...);                                 │
│   }                                                                      │
│                                                                          │
│   stopRotation() {                                                       │
│     clearInterval(this.intervalId);                                     │
│   }                                                                      │
│                                                                          │
│   destroy() {                                                            │
│     this.stopRotation();                                                │
│     this.currentIndex = -1;                                             │
│   }                                                                      │
│                                                                          │
│   updateBackgrounds(newBackgrounds) {                                   │
│     // NUEVO: Actualizar lista en runtime                               │
│   }                                                                      │
│ }                                                                        │
│                                                                          │
│ export default BackgroundRotator;                                       │
└─────────────────────────────────────────────────────────────────────────┘


FLUJO DE EJECUCIÓN
═══════════════════════════════════════════════════════════════════════════════

1. INICIALIZACIÓN
   ─────────────────────────────────────────────────────────────────────

   app-initialization.js  →  lazy-loader.js  →  frankenstein-ui.js
                                    ▲
                                    │ script.type = 'module'
                                    │ (modificado en lazy-loader)
                                    │
                            frankenstein-ui.js
                                    │
                                    │ import BackgroundRotator
                                    ▼
                      frankenstein-background-rotator.js


2. USO EN RUNTIME
   ─────────────────────────────────────────────────────────────────────

   FrankensteinLabUI.createStartScreen()
            │
            │ this.startBackgroundRotation()
            ▼
   [WRAPPER DEPRECADO]
            │
            │ this.backgroundRotator.startRotation(null, 45000)
            ▼
   BackgroundRotator.startRotation()
            │
            ├─→ BackgroundRotator.setRandomBackground()
            │        │
            │        ├─→ BackgroundRotator.resolveAssetUrl()
            │        │        │
            │        │        └─→ return absolute URL
            │        │
            │        └─→ document.documentElement.style.setProperty('--da-vinci-bg', ...)
            │
            └─→ setInterval(() => {
                    BackgroundRotator.setRandomBackground()
                }, 45000)


3. LIMPIEZA
   ─────────────────────────────────────────────────────────────────────

   FrankensteinLabUI.destroy()
            │
            │ this.backgroundRotator.destroy()
            ▼
   BackgroundRotator.destroy()
            │
            ├─→ BackgroundRotator.stopRotation()
            │        │
            │        └─→ clearInterval(this.intervalId)
            │
            └─→ this.currentIndex = -1


BENEFICIOS DE LA ARQUITECTURA
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────┐
│ ANTES               │
├─────────────────────┤
│ 1 archivo           │   DESVENTAJAS:
│ 8,375 líneas        │   - Código monolítico
│ Múltiples           │   - Difícil de testear
│ responsabilidades   │   - No reutilizable
│ Acoplado            │   - Alta complejidad
└─────────────────────┘

                ⬇  REFACTORING

┌─────────────────────┬─────────────────────┐
│ DESPUÉS             │                     │
├─────────────────────┼─────────────────────┤
│ frankenstein-ui.js  │  background-rotator │   VENTAJAS:
│ 8,315 líneas        │  151 líneas         │   ✓ Separación clara
│ Lógica principal    │  Rotación fondos    │   ✓ Testeable aislado
│ Delega backgrounds  │  Autocontenido      │   ✓ Reutilizable
└─────────────────────┴─────────────────────┘   ✓ Menor complejidad


MÉTRICAS DE CALIDAD
═══════════════════════════════════════════════════════════════════════════════

┌──────────────────────────┬────────┬─────────┬───────────┐
│ Métrica                  │ Antes  │ Después │ Mejora    │
├──────────────────────────┼────────┼─────────┼───────────┤
│ Líneas frankenstein-ui   │ 8,375  │ 8,315   │ -60 (-1%) │
│ Responsabilidades        │ 20+    │ 19      │ -1        │
│ Módulos independientes   │ 0      │ 1       │ +1        │
│ Tests unitarios          │ 0      │ 1       │ +1        │
│ Dependencias circulares  │ N/A    │ 0       │ ✓         │
│ Reusabilidad             │ Baja   │ Alta    │ ✓         │
│ Mantenibilidad (1-10)    │ 5      │ 7       │ +40%      │
└──────────────────────────┴────────┴─────────┴───────────┘


═══════════════════════════════════════════════════════════════════════════════
                              FIN DEL DIAGRAMA
═══════════════════════════════════════════════════════════════════════════════
